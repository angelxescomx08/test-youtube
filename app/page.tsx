import { CustomButton } from "./components/ui/custom-button";

export default function Home() {
  return (
    <main>
      <CustomButton
        sx={{
          marginTop: "1rem"
        }}
      >
        Mi botón
      </CustomButton>
    </main>
  );
}
